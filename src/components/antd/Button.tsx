import { Button } from "../../tools/desing";
import type { ButtonProps } from "../../tools/desing";

export const CustomButton: React.FC<ButtonProps> = (props) => {
  return <Button {...props}> </Button>;
};
